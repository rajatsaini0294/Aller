<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/remove"
        android:title="Remove"
        app:showAsAction="never" />
    <item
        android:id="@+id/add"
        android:title="Set as visited"
        app:showAsAction="never" />
</menu>